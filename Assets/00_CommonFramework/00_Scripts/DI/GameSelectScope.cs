using VContainer;
using VContainer.Unity;

namespace O2un.DI
{
    public sealed class GameSelectScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
